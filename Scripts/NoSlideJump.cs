using UnityEngine;
using System.Collections;

public class NoSlideJump : MonoBehaviour {

	//Empty class used to tag objects such that the player controller does not recognise them as a valid area to
	//jump off.
	// Update is called once per frame
	void Update () {
	
	}
}
